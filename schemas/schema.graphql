# import * from './generated/prisma.graphql'

type Query {
  humans(searchString: String): [Human!]!
  human(id: ID!): Human
}

type Mutation {
  createHuman(data: HumanCreateInput!): Human!
  addFather(childId: ID!, fatherId: ID!): Human
}
