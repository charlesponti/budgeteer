describe('App', function() {
  'use strict';

  var app;

  beforeEach(function() {
    app = require('../../app/app.jsx');
  });

  afterEach(function() {
    app = undefined;
  });

  describe('.getCSRF()', function() {
    it('should return the csrf token on the page', function() {

    });
  });

});
